"""Backward-compatible re-export — canonical module: :mod:`forestseg.pipeline.cli`."""

from forestseg.pipeline.cli import *
from forestseg.pipeline.cli import (
    LEGACY_RL_HISTORY_FUSION_PARAM_DEFAULTS,
    REQUIRED_FUSION_FIXED_PARAM_KEYS,
    REQUIRED_FUSION_GRID_KEYS,
    VALID_RL_SELECTION_METRICS,
    VALID_TRAIN_SELECTION_METRICS,
    WORK_FILES,
    RestoreOutputsError,
    _atomic_write_json,
    _copy_json_if_exists,
    _ensure_directory_writable,
    _feature_feedback_transforms,
    _load_rl_history,
    _normalize_legacy_rl_history_entry,
    _read_json,
    _require_json_copy,
    _resolve_bandit_config,
    _resolve_fusion_stage,
    _resolve_label_mode,
    _resolve_label_paths,
    _snapshot_required_json,
    _validate_fusion_param_candidates,
    _validate_fusion_param_value,
    _validate_fusion_params,
    _validate_non_negative_int,
    _validate_positive_float,
    _validate_ratio,
    _validate_rl_history_entry,
    _validate_stage_int,
    _write_json,
    cmd_build_feature_stack,
    cmd_build_spec_tex,
    cmd_check_label_points,
    cmd_postprocess_export,
    cmd_preflight_check,
    cmd_prepare_input,
    cmd_prepare_label_points,
    cmd_run_all,
    cmd_run_closed_loop,
    cmd_run_rl_fusion,
    cmd_train_or_load_dl,
    infer_to_files,
    load_points_json,
    main,
    resolve_scene_input,
    save_metrics,
    train_supervised_model,
)
