architecture: deeplabv3plus
encoder_name: resnet34
in_channels: 1
classes: 1
activation: null

train:
  epochs: 8
  learning_rate: 0.0003
  weight_decay: 0.0001
  batch_size: 8
  tile_size: 512
  stride: 384
  max_patches: 2500
  alpha_spec: 0.2
  beta_tex: 0.2
  aux_warmup_epochs: 3

infer:
  tile_size: 512
  stride: 384
  batch_size: 8
  mc_dropout_passes: 4
